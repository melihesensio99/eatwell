import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { dailyLogService } from '../api/dailyLog.service';
export function useDailyLog(date: string) { return useQuery({ queryKey: ['daily-log', date], queryFn: () => dailyLogService.get(date), staleTime: 30_000 }); }
export function useUpdateDailyLogItem(date: string) { const client = useQueryClient(); return useMutation({ mutationFn: ({ itemId, request }: { itemId: string; request: { quantityGrams: number; caloriesPer100Grams?: number | null; proteinPer100Grams?: number | null; carbohydratesPer100Grams?: number | null; fatPer100Grams?: number | null } }) => dailyLogService.updateItem(itemId, request), onSuccess: () => { client.invalidateQueries({ queryKey: ['daily-log', date] }); client.invalidateQueries({ queryKey: ['daily-log-summary', date] }); client.invalidateQueries({ queryKey: ['daily-log-history'] }); client.invalidateQueries({ queryKey: ['weekly-summary'] }); } }); }
export function useDeleteDailyLogItem(date: string) { const client = useQueryClient(); return useMutation({ mutationFn: dailyLogService.deleteItem, onSuccess: () => { client.invalidateQueries({ queryKey: ['daily-log', date] }); client.invalidateQueries({ queryKey: ['daily-log-summary', date] }); client.invalidateQueries({ queryKey: ['daily-log-history'] }); client.invalidateQueries({ queryKey: ['weekly-summary'] }); } }); }
