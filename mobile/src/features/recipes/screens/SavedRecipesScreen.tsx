import React from 'react';
import { Pressable, ScrollView, StyleSheet, Text, View } from 'react-native';
import { colors, radius, spacing, typography } from '../../../theme';
import { useRemoveRecipe, useSavedRecipes } from '../hooks/useSavedRecipes';
export function SavedRecipesScreen() { const query = useSavedRecipes(); const remove = useRemoveRecipe(); return <ScrollView style={styles.page} contentContainerStyle={styles.content}><Text style={styles.eyebrow}>TARİFLERİM</Text><Text style={styles.title}>Kaydettiğim tarifler</Text>{query.isLoading ? <Text style={styles.muted}>Tarifler yükleniyor…</Text> : null}{(query.data ?? []).map((recipe) => <View key={recipe.id} style={styles.card}><View style={styles.header}><Text style={styles.cardTitle}>{recipe.recipeName}</Text><Pressable onPress={() => remove.mutate(recipe.id)}><Text style={styles.remove}>Sil</Text></Pressable></View><Text style={styles.description}>{recipe.description}</Text><Text style={styles.meta}>{recipe.preparationMinutes + recipe.cookingMinutes} dakika · {recipe.servings} porsiyon</Text>{recipe.steps.slice(0, 3).map((step, index) => <Text key={`${index}-${step}`} style={styles.step}>{index + 1}. {step}</Text>)}</View>)}{!query.isLoading && (query.data ?? []).length === 0 ? <Text style={styles.muted}>Henüz kaydedilmiş tarif yok.</Text> : null}</ScrollView>; }
const styles = StyleSheet.create({ page: { flex: 1, backgroundColor: colors.background }, content: { padding: spacing.lg, paddingTop: spacing.xl, paddingBottom: 120 }, eyebrow: { ...typography.label, color: colors.coral, letterSpacing: 1.4 }, title: { ...typography.title, color: colors.text, marginTop: 4 }, card: { backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.border, borderRadius: radius.lg, padding: spacing.lg, marginTop: spacing.lg }, header: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' }, cardTitle: { ...typography.heading, color: colors.text, flex: 1 }, remove: { ...typography.label, color: colors.error }, description: { ...typography.body, color: colors.textMuted, marginTop: spacing.sm }, meta: { ...typography.label, color: colors.coral, marginTop: spacing.md }, step: { ...typography.body, color: colors.text, marginTop: spacing.sm }, muted: { ...typography.body, color: colors.textMuted, marginTop: spacing.lg } });
